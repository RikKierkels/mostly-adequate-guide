// Refactor to remove all arguments by partially applying the function.

// words :: String -> [String]

// Words receives the word to split as it's second and last arg. This is important as it's the last
// argument that's operated on
const words = split(' ');
